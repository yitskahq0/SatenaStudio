# TODO: Fix Empty Localhost Page

- [x] Fix Navbar.jsx: Replace `<link>` with `<Link>` and correct path to "/projects"
- [x] Update Project.jsx: Change image paths to match public/ file case (e.g., "/Project1.png")
- [x] Start Vite dev server with `npm run dev`
- [x] Verify app loads correctly and check for console errors
